for (let i = 0; i < 4; i++) {
  console.log(`-----Bắt đầu làm bài tập ${i}`);
  for (let index = 0; index < 5; index++) {
    // chạy từ 0,1,2,3,4
    console.log(`Đang làm bài ${index}`);
  }
  for (let x = 5; x >= 0; x--) {
    // chạy từ 5,4,3,2,1,0
    console.log(`Đang làm bài ${x}`);
  }
}
